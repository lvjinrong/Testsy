<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lvjinrong.kotlin">

    <!-- Include required permissions for Google Mobile Ads to run. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <!-- Include the AdActivity configChanges and theme. -->
        <activity
            android:name=".Kotlin1Activity"
            android:launchMode="singleTask"></activity>
        <activity
            android:name=".Kotlin2Activity"
            android:label="BasicActivity with FloatingActionButton"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Kotlin3Activity"
            android:label="Kotlin3">
        </activity>
        <activity android:name=".Kotlin4Activity" android:label="Kotlin4">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>